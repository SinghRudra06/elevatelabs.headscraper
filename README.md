# elevatelabs.headscraper
This is a Python made web scraper that scrapes headline from the news website in my case it's Times Of India.It sends an HTTP request to the website using the requests library and then parses the HTML content using BeautifulSoup .All extracted headlines are cleaned, stored in a list, and saved to a text file named newsheadline.txt,
